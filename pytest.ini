[pytest]
# 用例路径
testpaths = test_cases
# 用例命名规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 运行参数（仅保留-s显示日志，-v显示详细结果）
addopts = -s -v
# 测试标签（可用于筛选用例）
markers =
    product: 商品接口用例
    user: 用户接口用例
    cart: 购物车接口用例